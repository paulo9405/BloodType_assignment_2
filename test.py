from termcolor import colored


print(colored(f"3. Exit the programvvvvv.", "yellow"))

*Explanation:*
- The main function runs the program in a loop, allowing repeated interactions until the user chooses to exit.
- It displays the menu, validates the user input, and processes their choice.
- Depending on the choice, it fetches and displays the compatible blood types for donation or receptio